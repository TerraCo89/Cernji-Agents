# ============================================================================
# Resume Agent MCP Server Configuration
# ============================================================================
# Copy this file to .env and update with your settings
# DO NOT commit .env to version control (it may contain sensitive data)

# ============================================================================
# Storage Backend Selection
# ============================================================================
# Options: "file" (default) or "sqlite"
# - "file": Use YAML files (current system, no database needed)
# - "sqlite": Use SQLite database (requires migration)
STORAGE_BACKEND=file

# ============================================================================
# SQLite Configuration (only used if STORAGE_BACKEND=sqlite)
# ============================================================================
# Path to SQLite database file (relative to project root)
SQLITE_DATABASE_PATH=./data/resume_agent.db

# ============================================================================
# User Configuration
# ============================================================================
# User ID for multi-user support (future feature)
# Leave as "default" for single-user setup
USER_ID=default

# ============================================================================
# Claude CLI Configuration (Optional)
# ============================================================================
# Path to Claude CLI executable
# Leave commented out to use default ("claude" in PATH)
# CLAUDE_CODE_PATH=claude

# ============================================================================
# Migration Instructions
# ============================================================================
# To switch from file-based to SQLite storage:
#
# 1. Run the migration script:
#    uv run scripts/migrate_to_sqlite.py
#
# 2. Update this file to use SQLite:
#    STORAGE_BACKEND=sqlite
#
# 3. Restart the MCP server:
#    uv run resume_agent.py
#
# To rollback to file-based storage:
# 1. Change STORAGE_BACKEND=file
# 2. Restart the MCP server
#
# Both backends can coexist - switch between them by changing STORAGE_BACKEND
