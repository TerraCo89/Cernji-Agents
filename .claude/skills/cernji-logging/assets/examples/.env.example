# Cernji-Logging Configuration
# Copy this file to your project root as .env and customize

# ============================================
# Logging Configuration
# ============================================

# Log level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# Output format: json (production), text/pretty (development)
# Python: json, text
# TypeScript: json, pretty
LOG_FORMAT=json

# Log file path (optional, defaults to stdout)
# Ensure the directory exists before starting the app
LOG_FILE=logs/app.json.log

# ============================================
# Service Identification
# ============================================

# Service name (REQUIRED)
# Used to identify your service in logs
SERVICE_NAME=my-service

# Service version (optional)
# Helps track which version of your app generated logs
SERVICE_VERSION=1.0.0

# Environment (optional, defaults to 'dev')
# Common values: dev, staging, production
ENVIRONMENT=dev

# ============================================
# Usage Notes
# ============================================
#
# 1. For local development:
#    - Use LOG_FORMAT=text (Python) or LOG_FORMAT=pretty (TypeScript)
#    - Use LOG_LEVEL=DEBUG for verbose output
#    - Omit LOG_FILE to output to console
#
# 2. For production:
#    - Use LOG_FORMAT=json for log aggregation
#    - Use LOG_LEVEL=INFO or WARN
#    - Set LOG_FILE to write logs to disk for Filebeat pickup
#
# 3. Create log directory before starting:
#    mkdir -p logs
#
# 4. Ensure log files are writable:
#    chmod 755 logs
