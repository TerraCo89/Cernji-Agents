# LangGraph Configuration for Resume Agent
# Local development setup - no LangSmith API key needed for local server

# Backend API URL (server-side, used by Next.js API proxy)
LANGGRAPH_API_URL=http://localhost:2024

# Frontend API URL (client-side, must be absolute URL for SDK)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Agent/Assistant ID - use resume_agent as the primary agent
NEXT_PUBLIC_ASSISTANT_ID=resume_agent

# Production LangGraph Configuration (quickstart) - Uncomment to use
# LANGGRAPH_API_URL="https://my-agent.default.us.langgraph.app"
# NEXT_PUBLIC_API_URL="https://my-website.com/api"
# LANGSMITH_API_KEY="lsv2_..."

# Logging Configuration
# Service name for structured logs (ECS format)
SERVICE_NAME=agent-chat-ui

# Log level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# Log format: json (production), pretty (development)
LOG_FORMAT=json

# Log file path (JSON logs shipped to Elasticsearch via Filebeat)
LOG_FILE=logs/app.json.log

# Service version and environment
SERVICE_VERSION=1.0.0
ENVIRONMENT=development
